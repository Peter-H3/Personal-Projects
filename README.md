# Personal-Projects

# File Sorter

A python automation script that organises files in a directory by file extension (Audio, Documents, Images, Other, Videos)

What it does:

- Sorts files by extension
- Automatically creates folders for each file type
- Cross-platform (works with Windows, macOS and Linux)
